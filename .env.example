# ---------------------------------------------------------------------------
# DYUni - environment file
#
# Copy this file to `.env` and write your own values:
#     cp .env.example .env
#
# Do not add the file `.env` to the repository. It holds secret values.
# ---------------------------------------------------------------------------

# --- Database ---------------------------------------------------------------
# The application reads these values in the file `model/db.php`.

DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=dyuni
DB_USERNAME=root
DB_PASSWORD=
